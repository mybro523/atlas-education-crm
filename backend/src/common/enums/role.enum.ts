// Mirror of the Prisma `Role` enum. Keep these values IDENTICAL on both sides
// (and with the frontend) — do not rename without a coordinated change.
export enum Role {
  FOUNDER = 'FOUNDER',
  ADMIN = 'ADMIN',
  SALES_MANAGER = 'SALES_MANAGER',
  TEACHER = 'TEACHER',
  STUDENT = 'STUDENT',
}
